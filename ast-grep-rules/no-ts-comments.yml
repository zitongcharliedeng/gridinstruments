id: no-ts-comments
message: "Use literate prose instead of code comments. End the code block, write markdown, resume with a new code block targeting the same file."
severity: error
note: "Entangled concatenates multiple ``` {.typescript file=...} blocks in document order. Split your code, add prose (with MDN links, tables, images), then resume. See literate/app-core.lit.md 'Default Zoom' section for the pattern."
language: TypeScript
rule:
  kind: comment
