id: no-raw-classlist-remove
language: typescript
rule:
  all:
    - pattern: $EL.classList.remove($$$)
    - not:
        inside:
          pattern: $ACTOR.subscribe($$$)
          stopBy: end
files:
  - "_generated/main.ts"
severity: error
message: "classList.remove() is banned in main.ts — use XState actor.send() and handle DOM in the subscriber."
note: "All UI state must be managed by XState actors. See _generated/machines/ for examples."
